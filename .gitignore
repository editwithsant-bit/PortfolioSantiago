# Copy this file to the project root, next to package.json.
# It keeps local installs, build output, private env files, logs, and heavy local exports out of GitHub.

node_modules
.next
out
dist

.env
.env.local
.env*.local

.vercel
.netlify

npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

*.tsbuildinfo
Thumbs.db
.DS_Store

# Long-form videos are embedded from YouTube now.
# Keep the heavy local exports out of GitHub/Vercel.
public/videos/*.mp4

# Shorts are embedded from YouTube now too.
public/shorts/*.mp4
